# یادداشت: نگاهی عمیق به Reflection در دات‌نت

## Reflection چیست؟
Reflection در دات‌نت قابلیتی است که به ما اجازه می‌دهد در زمان اجرا، ساختار و متادیتای نوع‌ها (Types) را بررسی و حتی از آن‌ها استفاده کنیم. به کمک کلاس‌هایی مثل `System.Type`، `PropertyInfo` و `MethodInfo` می‌توانیم:

- نام، Propertyها، فیلدها و متدهای یک کلاس را بخوانیم.
- Attributeهایی که روی اعضای کلاس اعمال شده‌اند را کشف کنیم.
- نمونه‌ی جدیدی از یک کلاس بسازیم یا متدی را تنها با دانستن نامش فراخوانی کنیم.

این توانایی، برنامه را از محدودیت‌های زمان کامپایل خارج می‌کند و امکان رفتارهای پویا (Dynamic) را فراهم می‌آورد.

## چه کاربردهایی دارد؟
1. **بازتاب ساختار داده‌ها**: می‌توانیم در زمان اجرا بفهمیم یک شیء دقیقاً چه Propertyهایی دارد؛ این موضوع در ابزارهایی مثل Serializerها و Mapperها بسیار مهم است.
2. **Late Binding و Dynamic Invocation**: وقتی نوع دقیق در زمان کامپایل مشخص نیست، با `Activator.CreateInstance` و `MethodInfo.Invoke` می‌توانیم شیء بسازیم و متدها را فراخوانی کنیم؛ مشابه چیزی که در تمرین ۲ پروژه استفاده شده است.
3. **Attributeهای سفارشی**: تعریف Attribute برای افزودن متادیتای قابل خواندن در زمان اجرا. نمونه‌های کاربردی در این پروژه شامل `[Developer]` برای مستندسازی، `[MyRequired]` برای اعتبارسنجی، `[LogExecution]` برای لاگ‌گیری و `[MapTo]` برای نگاشت است.
4. **ابزارهای تست و DI**: فریم‌ورک‌هایی مثل NUnit یا xUnit با Reflection تست‌ها را کشف می‌کنند و Containerهای Dependency Injection سازنده‌ها و وابستگی‌ها را با همین تکنیک پیدا می‌کنند.
5. **تولید کد و پیکربندی پویا**: بسیاری از ORMها (مانند Entity Framework) با خواندن Attributeها یا کنوانسیون‌ها مدل پایگاه‌داده را استخراج می‌کنند. همچنین می‌توان براساس متادیتا، پیکربندی‌ها را در زمان اجرا تغییر داد.
6. **Pipelineهای تحلیلی**: با استفاده از Attributeها می‌توان پردازش‌های شرطی نوشت؛ مثلاً هر Property که `[MyRequired]` دارد باید قبل از ذخیره‌سازی اعتبارسنجی شود.

## نکات مهم هنگام استفاده
- Reflection هزینه‌ی عملکردی بیشتری نسبت به کد معمولی دارد؛ بنابراین تنها وقتی استفاده کنید که نیاز به پویایی دارید.
- خطاها تا زمان اجرا آشکار می‌شوند؛ حتماً سناریوهای failure را مدیریت کنید (مثلاً نبودن متد یا Property).
- برای کدنویسی تمیز، منطق Reflection را در کلاس‌های کمکی (Utility/Service) ایزوله کنید تا بقیه‌ی بخش‌های برنامه ساده بمانند.
- در صورت نیاز به عملکرد بالا، نتایج Reflection را cache کنید (مثل Type یا MethodInfo) تا از تکرار عملیات سنگین جلوگیری شود.

## پیوند با تمرین‌های پروژه
- **تمرین ۱**: نمایش Propertyها و متدها، نمونه‌ی کلاسیک Reflection است که فهم متادیتا را تقویت می‌کند.
- **تمرین ۲**: Late Binding را نشان می‌دهد؛ بدون دانستن نوع، شیء ساخته و متد اجرا می‌شود.
- **تمرین ۳ و ۴**: ساخت Attribute سفارشی برای مستندسازی و اعتبارسنجی، نمونه‌ی واضح از افزودن رفتار با متادیتاست.
- **تمرین ۵**: نشان می‌دهد چگونه می‌توان با Attribute و Reflection، لاگ‌گیری مقطعی (Cross-cutting concern) را پیاده کرد.
- **تمرین ۶**: Mapper ساده‌ای می‌سازد که به‌جای نگهداری نقشه‌ها در کد، از Attribute و نام Propertyها استفاده می‌کند.
- **تمرین ۷**: اسکن کل Assembly و اجرای خودکار متدهای علامت‌گذاری‌شده را تمرین می‌دهد؛ مشابه رفتار بسیاری از ابزارهای بوت‌استرپ.

این یادداشت می‌تواند همراه پروژه برای معرفی سریع Reflection و ایده‌های کاربردی آن استفاده شود.
